---
title: "Test 13: Z-test of a Correlation Coefficient"
output:
  word_document:
    highlight: "default"
    reference_docx: word_styles.docx
   
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r Step_1, warning=FALSE, message=FALSE}
# Load Required Functions:
source(file = "muFunc.R")

# Load Required Packages:
wants <- c("dplyr", "psych", "pacman", "ggplot2",
           "PerformanceAnalytics", "miscor")
has <- wants %in% rownames(x = installed.packages())
if(any(!has)) install.packages(wants[!has])
pacman::p_load(char = wants)

# Load Required Data:
data <- read.csv(file = "data/Data_Test_13.csv", header = TRUE)

# Show Data:
headTail(x = data, top = 2, bottom = 2)
```

```{r Step_2, fig.width=4, fig.height=4, dpi=600}
# Assumption Checking:
# 1. The x and y values originate from normal distributions.
#   1.1. Precipitation Mashhad
shapiro.test(data$Mashhad)

#   1.2. Precipitation Torbat Heydariyeh
shapiro.test(data$Torbat)

# 2. The relationship is linear.
chart.Correlation(data[, c("Mashhad", "Torbat")])

ggplot(data, aes(Torbat, Mashhad)) + geom_point() + 
  geom_smooth(method=lm) + xlim(50, 450) + ylim(50, 450)

```

```{r Step_3}
# Method 1:---------------------------------------------------------------------
x = data$Mashhad
x_bar = mean(x = x)

y = data$Torbat
y_bar = mean(x = y)

n = length(x)

rho0 = 0.70

# Correlation Coefficient (r)
r = (sum((x - x_bar) * (y - y_bar))) / (sqrt(sum((x - x_bar) ^ 2) * sum((y - y_bar) ^ 2)))

# Fisher Z-transformation:
Z1 = 0.5 * log((1 + r) / (1 - r))

# The distribution of Z1 is approximately normal with mean:
mu_Z1 = 0.5 * log((1 + rho0) / (1 - rho0))

# and standard deviation:
sd_Z1 = 1 / sqrt(n - 3)

# Test Statistic
Z = (Z1 - mu_Z1) / sd_Z1

# Compute the critical values at 0.05 significance level (two tailed).
alpha = 0.05
z.half.alpha = round(x = qnorm(p = (1 - alpha/2)), digits = 4)

# P-value for Spearman's rank correlation coefficient (Two Tailed).
p_value = 2 ∗ (1 - pnorm(q = Z))

cat("z-score: ", Z, "\n")
cat("Critical values: ", c(-z.half.alpha, z.half.alpha), "\n")
cat("P-value: ", p_value, "\n")

plotDistStat(dist = "Normal",
             to = 3,
             alpha_level = 0.05,
             statistic_point = Z,
             p_value = p_value)

# Method 2:---------------------------------------------------------------------
# setting initial parameter values:
r = cor(x = data$Mashhad, y = data$Torbat)
rho0 = 0.70
n = nrow(x = data)

# use test.core function:
test.cor(r = r, n = n, rho0 = rho0, conf.level = 0.90, reduced = TRUE)
```

